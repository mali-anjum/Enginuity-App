# Production — copy to `.env.production` for local builds that target production Supabase,
# or prefer EAS / CI secrets for release builds. Never commit `.env.production`.

EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROD_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=YOUR_PROD_SUPABASE_ANON_KEY
